{
  "name": "hwdc-2025-mcp-league-starter",
  "version": "2.1.1",
  "description": "HWDC 2025「Hello World」工作坊範例：MCP 大聯盟全端樣板",
  "private": true,
  "scripts": {
    "install:all": "pnpm install && cd backend && uv sync && cd ..",
    "setup:env": "cp .env.docker.example .env.docker && echo '\\n✅ 已複製 .env.docker.example 到 .env.docker\\n請編輯 .env.docker 並設置以下變數：\\n- OPENAI_API_KEY\\n- MCP_SERVER_AUTH_TOKEN\\n\\n可選變數：\\n- AS_A_MCP_SERVER（設為 true 將後端作為 MCP 服務器運行）\\n\\n生成安全令牌：openssl rand -base64 32'",
    "setup:env:local": "cd backend && cp .env.example .env && cd .. && echo '\\n✅ 已複製 backend/.env.example 到 backend/.env\\n請編輯 backend/.env 並設置以下變數：\\n- OPENAI_API_KEY\\n- MCP_SERVER_AUTH_TOKEN\\n\\n可選變數：\\n- AS_A_MCP_SERVER（設為 true 將後端作為 MCP 服務器運行）'",
    "setup:config": "cd backend && mkdir -p config && for file in defaults/default_*.json; do base=$(basename \"$file\" | sed 's/^default_//'); target=\"config/$base\"; if [ ! -f \"$target\" ]; then cp \"$file\" \"$target\" && echo \"✅ 複製 $file → $target\"; else echo \"⏭️  跳過 $target（檔案已存在）\"; fi; done && echo '\\n🎉 配置檔案設置完成！\\n您現在可以編輯以下檔案來自定義配置：\\n- backend/config/llm_models.json\\n- backend/config/active_llm_model.json\\n- backend/config/agno_prompts.json\\n- backend/config/agno_tools.json\\n- backend/config/mcp_servers.json\\n\\n注意：backend/config/ 目錄已被 .gitignore 忽略，您的修改不會被提交到 Git' && cd ..",
    "dev": "concurrently \"cd backend && uv run server.py\" \"pnpm --filter frontend dev\"",
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:backend": "cd backend && uv run server.py",
    "build": "cd backend && uv build && pnpm --filter frontend build",
    "build:frontend": "pnpm --filter frontend build",
    "build:backend": "cd backend && uv build",
    "test": "cd backend && uv run pytest && pnpm --filter frontend test",
    "test:frontend": "pnpm --filter frontend test",
    "test:backend": "cd backend && uv run pytest",
    "lint": "cd backend && uv run ruff check . && pnpm --filter frontend lint",
    "lint:frontend": "pnpm --filter frontend lint",
    "lint:backend": "cd backend && uv run ruff check .",
    "type-check": "cd backend && uv run mypy . && pnpm --filter frontend type-check",
    "type-check:frontend": "pnpm --filter frontend type-check",
    "type-check:backend": "cd backend && uv run mypy .",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:build": "docker compose build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/commit-analyzer": "^13.0.0",
    "@semantic-release/exec": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^11.0.0",
    "@semantic-release/npm": "^12.0.1",
    "@semantic-release/release-notes-generator": "^14.0.1",
    "conventional-changelog-conventionalcommits": "^8.0.0",
    "semantic-release": "^24.0.0"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@10.14.0",
  "keywords": [
    "hwdc",
    "hello-world",
    "mcp",
    "fullstack",
    "fastapi",
    "nextjs",
    "docker",
    "railway"
  ],
  "author": "",
  "license": "Apache-2.0"
}
