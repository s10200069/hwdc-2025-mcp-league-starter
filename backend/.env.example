# Server Configuration
HOST=0.0.0.0
# PORT for backend service
# - Use 8000 when running in Docker (with Nginx on 8080)
# - Use 8080 when running locally without Docker
PORT=8000
ENVIRONMENT=development

# CORS Configuration (Optional - has environment-specific defaults)
#
# Development environments: Defaults to localhost:3001, localhost:8080, localhost:3000
# Production environments: No defaults - must be explicitly configured
#
# Format: Comma-separated list of allowed origins
# Important: Always use quotes when setting via shell/Docker to protect special characters
#
# Examples:
# Simple domains:
# CORS_ALLOWED_ORIGINS=https://your-app.run.app,https://www.your-domain.com
#
# With query parameters or fragments (use quotes):
# CORS_ALLOWED_ORIGINS="https://app.example.com?tenant=foo,https://api.example.com#main"
#
# In .env file (quotes optional but recommended):
CORS_ALLOWED_ORIGINS=https://your-app.run.app,https://www.your-domain.com
#
# In Docker CLI (quotes REQUIRED):
# docker run -e "CORS_ALLOWED_ORIGINS=https://app.com,https://www.app.com"
#
# In docker-compose.yml (quotes REQUIRED):
# environment:
#   - "CORS_ALLOWED_ORIGINS=https://app.com,https://www.app.com"
#
# Advanced options (rarely needed - sensible defaults provided):
# CORS_ALLOWED_METHODS=GET,POST,PUT,PATCH,DELETE,OPTIONS
# CORS_ALLOWED_HEADERS=Authorization,Content-Type,Accept,Accept-Language,X-Requested-With

# Logging
LOG_LEVEL=INFO

# OpenAI API Key (get from https://platform.openai.com/api-keys)
OPENAI_API_KEY="your-openai-api-key-here"

# MCP (Model Context Protocol) Configuration
ENABLE_MCP_SYSTEM=true
MCP_TIMEOUT_SECONDS=60

# Note: MCP server commands and arguments are configured in config/mcp_servers.json
# Each server must specify:
#   - command: The executable to run (e.g., "npx")
#   - args: Array of arguments (e.g., ["-y", "@package/name"])
#   - enabled: Boolean to enable/disable the server
#   - timeout_seconds: Optional timeout override

# MCP Server Authentication (REQUIRED)
# IMPORTANT: This token is REQUIRED for MCP server authentication
# All requests to /mcp endpoint will require Bearer token authentication
# This is essential for secure peer-to-peer MCP connections between containers
#
# Generate a secure token using: openssl rand -base64 32
# Or use any secure random string generator
#
# Example:
# MCP_SERVER_AUTH_TOKEN=abc123def456ghi789jkl012mno345pqr678
MCP_SERVER_AUTH_TOKEN=your-secure-token-here-CHANGE-THIS
