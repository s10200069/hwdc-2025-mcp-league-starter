# ================================
# Environment Configuration
# ================================

# Application Environment
# Controls behavior and optimizations for different deployment stages
# Options: development | production | test | staging
# Default: Inherits from NODE_ENV if not set
# NEXT_PUBLIC_APP_ENV=production

# ================================
# Development Settings
# ================================

# Development Backend URL (for Next.js rewrites)
# Used only in development mode to proxy /api/* requests to backend server
# In production, Nginx handles routing, so this is ignored
#
# Default: http://localhost:8000 (matches backend dev server port)
# Adjust if your backend runs on a different port
DEV_BACKEND_URL=http://localhost:8000

# ================================
# API Request Timeouts
# ================================
# These control how long the frontend waits for API responses before timing out
# All values are in milliseconds

# Regular API requests timeout
# Used for: Standard CRUD operations, data fetching
# Default: 30000ms (30 seconds)
NEXT_PUBLIC_API_TIMEOUT=30000

# Long-running operations timeout
# Used for: MCP operations, file processing, complex queries
# Default: 120000ms (120 seconds / 2 minutes)
NEXT_PUBLIC_API_TIMEOUT_LONG_RUNNING=120000

# Conversation requests timeout
# Used for: AI chat responses, streaming operations
# Default: 180000ms (180 seconds / 3 minutes)
NEXT_PUBLIC_API_TIMEOUT_CONVERSATION=180000
